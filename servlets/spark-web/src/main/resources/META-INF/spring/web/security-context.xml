<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd">

    <http auto-config="true" use-expressions="true" xmlns="http://www.springframework.org/schema/security">
        <intercept-url pattern="/static/**" access="isAnonymous() or isFullyAuthenticated() or isRememberMe()" />
        <intercept-url pattern="/auth/**" access="isAnonymous() or isFullyAuthenticated() or isRememberMe()" />
        <intercept-url pattern="/**" access="isFullyAuthenticated()" />
        <form-login login-page="/auth/user-login" username-parameter="username"
            password-parameter="password" authentication-failure-url="/auth/user-login/failure" />
        <custom-filter before="FIRST" ref="redirectRootUriFilter" />
        <custom-filter after="LAST" ref="dualLoginCheckFilter" />
    </http>

    <authentication-manager xmlns="http://www.springframework.org/schema/security">
        <authentication-provider ref="authenticationProvider" />
    </authentication-manager>

    <bean id="authenticationProvider" class="org.springframework.security.authentication.dao.DaoAuthenticationProvider">
        <property name="userDetailsService" ref="userDetailsService" />
        <property name="passwordEncoder" ref="passwordEncoder" />
        <property name="hideUserNotFoundExceptions" value="false" />
        <property name="preAuthenticationChecks">
            <bean class="jp.gr.java_conf.star_diopside.spark.service.userdetails.BeforeLoginUserDetailsChecker" />
        </property>
    </bean>

    <bean id="redirectRootUriFilter" class="jp.gr.java_conf.star_diopside.spark.commons.web.servlet.filter.RedirectRootUriFilter" />

    <bean id="dualLoginCheckFilter" class="jp.gr.java_conf.star_diopside.spark.web.servlet.DualLoginCheckFilter" />

    <bean id="flashMapManager" class="org.springframework.web.servlet.support.SessionFlashMapManager" />

</beans>
