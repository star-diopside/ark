machine:
  java:
    version: oraclejdk8
  environment:
    TEST_ENVIRONMENT: circleci
database:
  override:
    - ./gradlew -b config/database/build.gradle -Penv=circleci update
dependencies:
  pre:
    - ./ci/scripts/install_repository.sh
